services:
  api:
    build: .
    command: uvicorn dealflow_ai_engine.api.app:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
