name: deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t dealflow-ai-engine:${{ github.sha }} .
      - run: echo "Deploy application container and Airflow DAG package to target environment."
