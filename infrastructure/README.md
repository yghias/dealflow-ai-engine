# Infrastructure

This directory contains infrastructure notes and Terraform definitions for the platform runtime. Snowflake is treated as the primary warehouse, Airflow is the orchestrator, and the API service can run as a lightweight containerized control plane.
